#include "main.h"

/**
 *void print_numbers(void) - program that prints prints the numbers, from 0 to 9
 *
 *
 *Return: Always 0 (Success)
 */

void print_numbers(void);
{
  int i = 0;
    for (i = 0; i <= 9; i++)
    _putchar (i);
    _putchar ('\n');
}
